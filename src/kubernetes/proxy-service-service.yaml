apiVersion: v1
kind: Service
metadata:
  name: proxy-service
  namespace: cinemaabyss
spec:
  selector:
    app: proxy-service
  ports:
    - protocol: TCP
      port: 8080        # Порт сервиса, который смотрит Ingress
      targetPort: 8000  # Порт контейнера